# ElektroStorage

En simpel lagerapp lavet med Spring Boot, JPA, H2 og JavaScript.

## Sådan startes programmet

Kør:

```bash
mvn spring-boot:run
```

Åbn derefter `http://localhost:8080`.

Tests køres med `mvn test`.
